{
  "name": "database-emulator-javascript-quickstart",
  "version": "1.0.1",
  "description": "Real Time Database emulator testing",
  "scripts": {
    "format": "prettier --write **/*.js",
    "test": "mocha --timeout=10000"
  },
  "devDependencies": {
    "@firebase/testing": "^0.15.0",
    "mocha": "5.2.0",
    "prettier": "1.15.2"
  }
}
