{
  "name": "messaging",
  "scripts": {
    "compile": "cp ../tsconfig.template.json ./tsconfig.json && tsc"
  },
  "dependencies": {
    "google-auth-library": "^8.8.0",
    "googleapis": "^118.0.0"
  }
}
